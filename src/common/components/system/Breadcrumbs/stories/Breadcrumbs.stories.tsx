import React, { ReactElement } from 'react';

import { Breadcrumbs } from '../Breadcrumbs';

export default {
  title: 'Components/Breadcrumbs',
  component: Breadcrumbs,
};

export const Basic = (): ReactElement => <Breadcrumbs>Basic</Breadcrumbs>;
