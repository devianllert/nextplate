
import React, { ReactElement } from 'react';

import Skeleton from '../Skeleton';

export default {
  title: 'Components/Skeleton',
  component: Skeleton,
};

export const Basic = (): ReactElement => <Skeleton>Basic</Skeleton>;
