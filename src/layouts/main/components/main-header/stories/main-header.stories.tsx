import React, { ReactElement } from 'react';

import { MainHeader } from '../main-header';

export default {
  title: 'Layouts/Main/MainHeader',
  component: MainHeader,
};

export const Basic = (): ReactElement => <MainHeader />;
