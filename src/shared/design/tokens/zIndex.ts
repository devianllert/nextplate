// We need to centralize the zIndex definitions as they work
// like global values in the browser.
export const zIndex = {
  appBar: 1100,
  drawer: 1200,
  modal: 1300,
  snackbar: 1400,
  tooltip: 1500,
};
