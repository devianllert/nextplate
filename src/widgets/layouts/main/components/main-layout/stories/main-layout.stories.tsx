import React, { ReactElement } from 'react';

import { MainLayout } from '../main-layout';

export default {
  title: 'Layouts/Main/MainLayout',
  component: MainLayout,
};

export const Basic = (): ReactElement => <MainLayout>Basic</MainLayout>;
